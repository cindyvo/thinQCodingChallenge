export default class JwtBearerOptions {
    authority: string;
    requireHttpsMetadata: boolean;
    audience: string;
    checkAudience: boolean;
    constructor(authority: string, requireHttpsMetadata?: boolean, audience?: string, checkAudience?: boolean);
}
